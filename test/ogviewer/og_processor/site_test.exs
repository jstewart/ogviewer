defmodule Ogviewer.OgProcessor.SiteTest do
  use Ogviewer.DataCase

  describe "fetching a URL" do
    test "errors with an invalid url"
    test "errors with a non-successful HTTP status"
    test "errors with a timeout"
    test "network errors"
  end
end
